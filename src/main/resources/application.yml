spring:
  datasource:
      driver-class-name: org.postgresql.Driver
      username: pivocore
      password: password
      url: jdbc:postgresql://localhost:5432/pivocoredb
  liquibase:
    change-log: classpath:liquibase/database-changelog.xml
  banner:
    location: classpath:banner.txt

server:
  port: 8080

jwt:
  secret: super-secret